# Ejemplo Angular v4 + testing

Esto es un ejemplo universal para mis charlas de introducción a Angular y testing.

Las slides (con los plunkers) están [aquí](https://slides.com/jesusrodriguez-3/opensouthcode-angular)

Para usar este proyecto, clonad el repo y haced un:

```
$ npm install
$ ng serve
```

Y desde [localhost:4200](localhost:4200) podéis probarlo.

# Angular v4 example + testing

This is an universal example I use on my talks about Angular and testing (In spanish for now).

The slides (with plunkers) are [here](). Also in Spanish.

To use this project, clone the repo and do:

```
$ npm install
$ ng serve
```

Use it from [localhost:4200](localhost:4200).
